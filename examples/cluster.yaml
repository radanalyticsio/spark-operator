apiVersion: v1
kind: ConfigMap
metadata:
  name: my-spark-cluster
  labels:
    radanalytics.io/kind: sparkcluster
data:
  config: |-
    worker:
      instances: "2"
    master:
      instances: "1"
