apiVersion: radanalytics.io/v1
kind: SparkCluster
metadata:
  name: my-spark-cluster-limreq
spec:
  worker:
    instances: "1"
    cpu: 150m
    memory: 250m
    cpuRequest: 100m
    memoryRequest: 200m
  master:
    instances: "1"
