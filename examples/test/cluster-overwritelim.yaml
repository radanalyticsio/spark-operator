apiVersion: radanalytics.io/v1
kind: SparkCluster
metadata:
  name: my-spark-cluster-overwritelim
spec:
  worker:
    instances: "1"
    cpu: 150m
    memory: 250m
    cpuLimit: 175m
    memoryLimit: 275m
  master:
    instances: "1"
